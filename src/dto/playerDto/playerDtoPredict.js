export default class PlayerDtoPredict {
  constructor(id,name,order,handId,predict,take){
    this.id = id;
    this.name = name;
    this.order = order;
    this.handId = handId;
    this.predict = predict;
    this.take = take;
  }
}