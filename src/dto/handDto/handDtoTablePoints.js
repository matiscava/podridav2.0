export default class HandDtoTablePoints{
  constructor(handNumber,predict,take,points){
    this.handNumber = handNumber;
    this.predict = predict;
    this.take = take;
    this.points = points;
  }
} 