export default class HandDtoPoints{
  constructor(name, order, predict, take, points){
    this.name = name;
    this.order = order;
    this.predict = predict;
    this.take = take;
    this.points = points;
  }
}